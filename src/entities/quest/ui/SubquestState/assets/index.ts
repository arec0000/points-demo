import { ActualState } from './ActualState';
import { FinishedState } from './FinishedState';
import { UnavailableState } from './UnavailableState';

export const SubquestStates = {
  actual: ActualState,
  unavailable: UnavailableState,
  finished: FinishedState,
};
